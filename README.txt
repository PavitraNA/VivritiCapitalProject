1.  The Code was developed using the Visual Studio.
2. To run the code You will have to 1st Install the node modules.
3. npm install will allow you to install all the required Node packages required by the program mentioned in Package.json.
4. Once all the modules are installed to run the code you will have to use npm start command
5. all these npm command can be done on a simple cmd.
